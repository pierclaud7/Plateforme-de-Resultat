{{ form_start(form) }}

    <div class="mb-3">
        {{ form_label(form.intitule, 'Nom du Diplôme', {'label_attr': {'class': 'form-label fw-bold'}}) }}
        {{ form_widget(form.intitule, {'attr': {'class': 'form-control', 'placeholder': 'Ex: BTS SIO'}}) }}
        {{ form_errors(form.intitule) }}
    </div>

    <div class="mb-3">
        {{ form_label(form.filiere, 'Filière associée', {'label_attr': {'class': 'form-label fw-bold'}}) }}
        {{ form_widget(form.filiere, {'attr': {'class': 'form-select'}}) }}
        <div class="form-text text-muted">Sélectionnez la filière à laquelle ce diplôme appartient.</div>
        {{ form_errors(form.filiere) }}
    </div>

    <button class="btn btn-primary w-100 mt-3 shadow-sm">
        <i class="fas fa-save"></i> {{ button_label|default('Enregistrer') }}
    </button>

{{ form_end(form) }}
