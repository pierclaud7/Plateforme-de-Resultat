{% extends 'base.html.twig' %}

{% block title %}Détails de la Filière{% endblock %}

{% block body %}
    <h1>Détails de la Filière</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ filiere.id }}</td>
            </tr>
            <tr>
                <th>Nom de la filière</th>
                <td>{{ filiere.nom }}</td>
            </tr>
        </tbody>
    </table>

    <br>

    <a href="{{ path('app_filiere_index') }}">Retour à la liste</a>

    <a href="{{ path('app_filiere_edit', {'id': filiere.id}) }}" style="margin-left: 15px;">Modifier</a>

    <br><br>

    {{ include('filiere/_delete_form.html.twig') }}
{% endblock %}
