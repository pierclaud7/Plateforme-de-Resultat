{{ form_start(form) }}
    <div class="mb-3">
        {{ form_label(form.nom, 'Nom de la filière', {'label_attr': {'class': 'form-label fw-bold'}}) }}
        {{ form_widget(form.nom, {'attr': {'class': 'form-control', 'placeholder': 'Ex: Informatique'}}) }}
        {{ form_errors(form.nom) }}
    </div>
    <button class="btn btn-primary w-100 mt-3 shadow-sm">
        <i class="fas fa-save"></i> {{ button_label|default('Enregistrer') }}
    </button>
{{ form_end(form) }}
